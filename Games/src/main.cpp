import Game.Animation;

// TODO Ideas
// Set Camera to be in pixels ? Or to be in whichever format I prefer! 
// The class camera needs to store the current window size for that!
// - FSM class?
// - Entrypoint? Maybe not needed!

import std;

int main()
{
    Main();
    return 0;
}